#include "ExitMapper.h"

void ExitMapper::SetCloud(pcl::PointCloud<pcl::PointXYZ> cloud) {}

ExitMapper::ExitMapper() {}

void ExitMapper::Run() {}

pcl::PointXYZ ExitMapper::FindExit() { return 0; }

bool ExitMapper::ExitReady() { return true; }
